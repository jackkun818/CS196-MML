<UserControl
    x:Class="crs.extension.Controls.MoCAs.Subject_7"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:controls="clr-namespace:crs.extension.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:crs.extension.Controls.MoCAs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:crs.extension.Models"
    xmlns:theme="clr-namespace:crs.theme;assembly=crs.theme"
    d:Background="White"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True,
                                     Type={x:Type models:SubjectItem}}"
    d:DesignHeight="579"
    d:DesignWidth="1400"
    mc:Ignorable="d">

    <DockPanel>
        <TextBlock
            DockPanel.Dock="Top"
            TextBlock.FontSize="32"
            TextBlock.FontWeight="Bold"
            TextTrimming="CharacterEllipsis">
            <behaviors:Interaction.Triggers>
                <behaviors:EventTrigger EventName="Loaded">
                    <behaviors:ChangePropertyAction
                        PropertyName="OriginName"
                        TargetObject="{Binding}"
                        Value="language" />
                </behaviors:EventTrigger>
            </behaviors:Interaction.Triggers>
            language
        </TextBlock>
        <StackPanel
            Margin="0,30,0,0"
            VerticalAlignment="Center"
            DockPanel.Dock="Top"
            Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" FontSize="28">Now I say something, please repeat it clearly, this sentence is:</TextBlock>
            <ContentControl
                Margin="10,0,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                ContentTemplate="{StaticResource MMSE_MoCA_ToolTipTemplate}">
                <ContentControl.Content>
                    <TextBlock TextTrimming="CharacterEllipsis" TextWrapping="Wrap">
                        Rating: The retelling is correct, and each sentence is given 1 point. Retelling must be accurate. Pay attention to the omissions that occur during retelling（For example, omitted”Only“，”always“）And replacement/Increase（like I only know Zhang Liang today......“Say it”I know Zhang Liang today......“；or”Room“Say it”house“wait）
                    </TextBlock>
                </ContentControl.Content>
            </ContentControl>

        </StackPanel>
        <UniformGrid
            Width="1060"
            Height="240"
            VerticalAlignment="Center"
            Columns="1"
            Rows="2"
            TextBlock.FontSize="36"
            TextBlock.FontWeight="Bold">
            <DockPanel DataContext="{Binding ChildrenItems[0]}">
                <ContentControl
                    Margin="30,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Content="{Binding AnswerItems[0]}"
                    ContentTemplate="{StaticResource MMSE_MoCA_AnswerTemplate}"
                    DockPanel.Dock="Right" />
                <hc:TextBox
                    Width="230"
                    Height="50"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    hc:InfoElement.ShowClearButton="True"
                    BorderBrush="Black"
                    DataContext="{Binding AnswerItems[0]}"
                    DockPanel.Dock="Right"
                    FontSize="20"
                    Text="{Binding Remark, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TextAlignment="Center" />
                <TextBlock
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis">
                    “I only know that Li Ming was a helper today”
                </TextBlock>
                <Button
                    Width="51"
                    Height="36"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="ExecuteAudioPlayClick"
                    Cursor="Hand"
                    Tag="I want to read a sentence below, please repeat it clearly. This sentence is: I only know that Li Ming is a person who has helped today">

                    <Button.Content>
                        <Image
                            Width="28"
                            Height="24"
                            VerticalAlignment="Center"
                            Cursor="Hand"
                            Source="{StaticResource crs_sound}"
                            Stretch="Uniform" />
                    </Button.Content>
                </Button>
            </DockPanel>
            <DockPanel DataContext="{Binding ChildrenItems[1]}">
                <ContentControl
                    Margin="30,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Content="{Binding AnswerItems[0]}"
                    ContentTemplate="{StaticResource MMSE_MoCA_AnswerTemplate}"
                    DockPanel.Dock="Right" />
                <hc:TextBox
                    Width="230"
                    Height="50"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    hc:InfoElement.ShowClearButton="True"
                    BorderBrush="Black"
                    DataContext="{Binding AnswerItems[0]}"
                    DockPanel.Dock="Right"
                    FontSize="20"
                    Text="{Binding Remark, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TextAlignment="Center" />
                <TextBlock
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis">
                    “When the dog is in the room, the cat always hides under the sofa”
                </TextBlock>
                <Button
                    Width="51"
                    Height="36"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="ExecuteAudioPlayClick"
                    Cursor="Hand"
                    Tag="I want to read a sentence below, please repeat it clearly. This sentence is: When a dog is in the room, the cat always hides under the sofa">

                    <Button.Content>
                        <Image
                            Width="28"
                            Height="24"
                            VerticalAlignment="Center"
                            Cursor="Hand"
                            Source="{StaticResource crs_sound}"
                            Stretch="Uniform" />
                    </Button.Content>
                </Button>
            </DockPanel>
        </UniformGrid>
    </DockPanel>

</UserControl>
